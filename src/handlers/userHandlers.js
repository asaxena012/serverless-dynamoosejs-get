const fetchUserDetails = (queryStringParameters, pathParameters, isBase64Encoded) => {
    // TODO :: Extract Id from path params
}

const fetchUsers = (queryStringParameters, pathParameters, isBase64Encoded) => {
    // TODO :: Extract querys params
}

module.exports = {
    fetchUserDetails,
    fetchUsers
}